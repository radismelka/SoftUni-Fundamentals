circle = list(map(int, input().split()))  # numbers separated by a single space 
killing_steps = int(input())

order_of_executions = []

is_finished = False

index = killing_steps - 1  # because it start counting from zero
if index >= len(circle):  # verification does the index is in the range
    index %= len(circle)

if len(circle) == 1:
    print(f"[{circle[0]}]")
else:
    while len(circle) > 0:
        order_of_executions.append(circle.pop(index))
        index += killing_steps - 1
        if index >= len(circle):
            if len(circle) == 0:
                is_finished = True
                break
            else:
                index %= len(circle)

if is_finished:
    print(f"[{','.join(map(str, order_of_executions))}]")
